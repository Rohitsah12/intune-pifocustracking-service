# ---- Build outputs (regenerated by Build-Package.ps1; never commit) ----
_package_*/
_package_*.zip
dist_intunewin/

# Stray artifact from an old bad path literal ("C:\ProgramData\PiFocusStage\selftest.log")
ProgramDataPiFocusStageselftest.log
*.selftest.log

# ---- Per-developer / editor config ----
.claude/

# ---- Real device + user PII: Intune device IDs, Azure AD tenant IDs, SIDs,
#      employee UPNs, BIOS serials. Do NOT publish these to a git remote.
#      (Sanitize first if a doc genuinely needs to be shared.) ----
backend-handoff-shared-pc-attribution/
backend-handoff-shared-pc-attribution.zip
poc-shared-pc-user-mapping/logs/
poc-shared-pc-user-mapping*.zip
